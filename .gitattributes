# 换行符：脚本必须是 LF，否则 macOS / Linux 上会报 bad interpreter
* text=auto eol=lf
*.bat  text eol=crlf
*.ps1  text eol=crlf

# 二进制，别做任何换行转换，也别在 diff 里展开
*.png  binary
*.gif  binary
*.wav  binary
*.webm binary
*.mov  binary
*.mp4  binary
